import subprocess

subprocess.run(['pip', 'install', 'ultralytics'], check=True)
subprocess.run(['pip', 'install', 'tkinter'], check=True)
subprocess.run(['pip', 'install', 'opencv-python'], check=True)
#subprocess.run(['pip', 'install', 'opencv-python-headless'], check=True)


#pyautogui
#!pip install -U sahi